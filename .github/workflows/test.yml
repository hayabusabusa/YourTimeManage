name: Test

on:
  pull_request:
    branches: [ main ]
    types: [ opened, synchronize ]

jobs:
  test:
    runs-on: macos-latest

    steps:
    - name: Checkout Repo
      uses: actions/checkout@v2
      
    - name: Run test
      run: |
        xcodebuild test -scheme YourTimeManage -destination "platform=iOS"
