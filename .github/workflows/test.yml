name: Test

on:
  pull_request:
    branches: [ main ]
    types: [ opened, synchronize ]

jobs:
  test:
    runs-on: macos-latest

    steps:
    - name: Checkout Repo
      uses: actions/checkout@v2
      
    - name: Run test
      run: |
         swift test
#        xcodebuild test -scheme YourTimeManage -destination "platform=iOS Simulator,name=iPhone 12"
